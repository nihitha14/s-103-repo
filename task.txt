1) git initialization --global user.name "githubusername"
   command needed:
     working directory:\>git init
2) check the git ststus (to know about untracked/unstaged files available in working directory)
   command needed
   working directory:\>git status
3) adding untracked files to the staging area:
   command needed:
   working directory:\>git add . (for adding all the files available in working directory)
   working directory:\>git add <file name> (for adding specific files)
4)commiting files to the local repository from staging area:
  command  needed:
  working directory:\>git commit -m "commit_message"
 here mm denotes message.
5) to check log of all commits:
      commit needed:
   working directory:\>git log --oneline


